# 10-里程碑与进度

> 本文件是进度唯一事实源。「当前状态」块是每次会话的交接锚点：开始时读，结束时写。

## ROADMAP

| 里程碑 | 波次 | 要点 | 状态 |
| --- | --- | --- | --- |
| M0 | - | 建仓与环境准备：工程骨架、文档体系、CI、竞品词拦截、Go 环境、最小可运行入口（/health） | ✅ 完成（2026-09-02） |
| M1 | wave1 | 存储层：六表模型与迁移框架、options 双轨热更 | ✅ 完成（2026-09-02） |
| M1 | wave2 | relay：双协议端到端转发、渠道路由与熔断 | ✅ 完成（2026-09-02） |
| M1 | wave3 | 计费内核：三模式引擎、预扣费与多退少补、流中断退款、未配价硬拒绝、内置价单、异步日志、黄金测试集——**M1 里程碑至此完成** | ✅ 完成（2026-09-02） |
| M2 | wave1 | 管理面 API：登录/会话（HMAC 签名 cookie、auth_version 失效）、渠道/令牌/用户/兑换码/日志/options 六组端点与热更闭环、渠道连通测试、限流挂接转发链路（全局/分组/令牌 TPM/RPM）、令牌分组接 GroupRatio | ✅ 完成（2026-09-03） |
| M2 | wave2 | React 管理台五页面对接管理面 API——实际交付八页面（含数据看板与模型广场） | ✅ 完成（2026-09-03） |
| M2 | wave3 | 兑换码核销状态机（兑换/划转/充值页）、令牌预算周期惰性重置、hooks 首批集成（OTLP/webhook）、流中断部分结算——**M2 里程碑至此完成** | ✅ 完成（2026-09-03） |
| M3 | - | 用户体系完善（注册发放业务化）与业务功能收尾：wave1 schema/公开注册、wave2 OAuth/2FA/个人中心、wave3 支付对接与邀请返利、wave4 worker/bundle 优化/个人日志视角——**M3 里程碑至此完成** | ✅ 完成（2026-09-03） |
| M4 | - | 端口接管切换演练 + 迁移工具（go:embed 管理台已于 M2-wave2 提前交付） | 🔄 进行中（2026-09-04 wave1：只读纪律+迁移工具+演练；wave2：生产旁路部署+切换 runbook，见交接记录） |
| M5 | - | 生产收尾：备份运维常态化、可观测完善、文档定稿 | ⬜ 未开始 |

> 各里程碑的详细设计见 docs/00（总路线）、docs/01（架构）、docs/03-05（领域设计）、docs/06（部署）。
> ROADMAP 重排说明（2026-09-02）：原 M2「计费内核」已由 M1-wave3 提前完成并并入 wave3 行；
> M2 重定义为管理面 API（原属 M3），M3 收敛为业务功能（预算/兑换码/用户与分组）。
> 波次重排说明（2026-09-03，M2-wave1）：M2 拆为三波——wave1 管理面 API（已完成，含令牌
> 分组接 GroupRatio 与限流挂接）、wave2 前端控制台五页面、wave3 兑换码核销状态机与
> 预算周期（原 M3 主体并入）；M3 收敛为用户体系业务化收尾。
> 范围修订说明（2026-09-03，M2-wave2）：go:embed 管理台原规划于 M4，随 wave2 一并交付，
> M4 相应收敛为端口接管与迁移工具；wave2 实际交付八页面（任务五页面 + 数据看板 + 模型广场）。

## 当前状态

- **已完成**：M1 全部三波 + M2 全部三波（M2 收官，含缺陷修复 Task #18/#19）+
  **M3-wave1 schema v4 与公开注册体系** + **M3-wave2 OAuth/2FA/个人中心**（细节见下方
  交接记录）：
  - schema v4（迁移 0004_m3_commercial）：users 加邀请/两步验证 5 列（aff_code/
    inviter_id/aff_history_quota/totp_secret（json:"-" 序列化豁免）/totp_enabled）+
    新表 user_identities（复合唯一 provider+provider_uid）/ topup_orders（订单号唯一
    索引、状态机 1=待支付 2=已支付 3=失败 4=过期）；TestDDLEquivalence 双源对账同步八表；
  - options 白名单扩 8 前缀（smtp./register./oauth./turnstile./epay./stripe./aff./
    topup.）+ 敏感键读取脱敏（键含 password/secret 恒回 `******`）+ PUT 哨兵跳过
    （回写 `******` = 保持旧值）；前端 Settings KEY_GROUPS 同步 8 分组（敏感键
    「已脱敏」标注）；
  - internal/mailer（465 隐式 TLS + AUTH LOGIN 手工实现，配置闭包热读取）与
    internal/verification（验证码内存存储：TTL 10min/重发 60s/日限 20/一次性消费/
    惰性 + 定时清扫）；
  - 公开注册四端点（GET /api/setup、POST /api/user/register、POST
    /api/verification_code、POST /api/user/reset_password）+ Login/reset IP 限频
    （register 1h×5 / login 1h×10 / reset 1h×5）+ Turnstile siteverify 接口化客户端
    （5s 超时，可 mock）；注册事务建户 + aff 双向奖励（并发恰一入账 + 两条
    protocol="aff" 日志）+ auth_version++ 会话失效；/api/status features 块；
  - 验收：`go test ./... -race` 15 包全绿（api 41 例含注册矩阵/Turnstile mock/
    IP 限频；verification/mailer 新包各 9/7 例）；vet 零告警；gofmt 干净；
    npm lint/build 通过；竞品词扫描通过；文档同步完成（docs/03/05/10/11/CHANGELOG）；
- **M3-wave2 OAuth + 2FA + 个人中心（Task #20，落于 2026-09-03）**：
  - OAuth 通用 provider（internal/api/oauth.go）：github/linuxdo/oidc 三 provider
    （配置键 oauth.*.client_id/secret + oauth.oidc.issuer）；authorize（32hex state
    一次性 HttpOnly cookie 5min 防 CSRF）/callback（state 恒时比较 → token →
    userinfo；oidc 走 .well-known 发现并缓存 1h）/bind 绑定模式（永不顶替会话）/
    解绑防锁死（identity_last）/身份列表；未命中+注册开放自动建户（事务原子、
    撞名回滚）；redirect_uri 信任 X-Forwarded-Proto；
  - 2FA TOTP（internal/api/totp.go，pquerna/otp v1.5.0）：setup/enable/disable 三端点
    （issuer="Hui Api"；disable 必验码双列清空）；
  - 二段式登录：session Stage claim（0 完整/1 待 2FA）+ Issue per-call TTL；Login 对
    TOTP 用户签 stage1 会话（5min）返回 {require_2fa:true}；POST /api/user/login/2fa
    重签完整会话；middleware RequireAuth 校验 Stage==0（stage1 访问自服务 401
    totp_required 防提权）；
  - 个人中心（internal/api/profile.go）：POST /api/user/password（旧口令校验；OAuth
    无口令账号首设免验；auth_version++ 并重签当前会话）与 /api/user/email（查重 409）；
  - 前端：Login 二段式 + OAuth 按钮（按 /api/setup 渲染）+ 注册链接；新建 Register.tsx
    （验证码 60s 倒计时 + Turnstile widget 轻量封装 + aff 预填）；新建 Profile.tsx
    （改密/改邮箱/2FA 三态管理/身份列表+解绑+绑定跳转）；App 路由（/register、
    /console/profile）；ConsoleLayout 菜单；
  - 验收：api 包新增 17 例（state CSRF 篡改/自动建户事务撞名回滚/注册关闭/命中登录/
    禁用拒绝/bind 流程/身份防锁死/OIDC 发现缓存/setup 探测/TOTP 全链路/二段式防提权/
    错码/disable 后免验/改密改邮箱）；`go test ./... -race` 全绿；vet 零告警；
    npm lint/build 通过；竞品词扫描通过。
- **M3-wave3 支付对接 + 邀请返利（Task #21，落于 2026-09-03）**：
  - internal/payment 适配层（零 SDK 依赖）：epay.go（MD5 签名：参数名字典序 k=v&
    末尾接 key、空值与 sign/sign_type 不参与、hex 小写；SubmitURL 构造；NotifyVerify
    恒时比较）+ stripe.go（Checkout Session：form 编码 + Basic secret_key +
    price_data 自定义金额 + metadata.order_no 回传；WebhookVerify：t/v1 解析 +
    HMAC-SHA256 恒时比较 + ±5min 防重放 + 多 v1 任一匹配）；
  - internal/api/order.go 六端点（下单/订单列表/epay notify+return/stripe webhook）
    + aff.go 邀请信息端点 + settleTopupOrder 共享结算事务（网关/币种/金额逐位校验
    → 条件 UPDATE pending→paid RowsAffected=0 幂等跳过 → 买家入账 → topup 日志 →
    aff 返利 (quota×pct+50)/100 同事务累加 aff_history_quota + protocol="aff" 日志）；
    汇率换算 quota=(cents×500000+rate/2)/rate，rate 定点快照同记订单（epay=缺省 720，
    stripe=100）；stripe 先建 session 后落库防孤儿单；
  - 前端：TopUp 在线充值区块（/api/setup topup 开关才渲染 + 网关 Radio + 金额元→分
    + 整页跳转 pay_url + 回跳 ?order= 查单 Alert）+ 充值订单 Table；新建 Invite 页
    （aff_code/邀请链接 ?aff= 复用 Register 预填/复制/累计返利/邀请人数/返利比例）+
    路由菜单（/console/invite 全角色）；types/format 扩展；
  - 遗留项加固随波清偿：TestBudgetConcurrentSingleReset 排空异步日志后断言
    （g.Close() 消除时序抖动）+ mailer 测试 gofmt 归一；
  - 验收：payment 包 5 测试（MD5 黄金向量双向互证/验签篡改矩阵/SubmitURL 形态/
    Webhook 三态与边界/Checkout 请求形态含 Basic 断言）；api 包新增 9 测试（下单
    校验矩阵/双网关下单快照/12 goroutine 重复通知恰一次入账含返利/验签与金额篡改
    拒绝/Stripe 验签正错过期三态/订单归属越权/aff 惰性码与二次稳定/return 302）；
    `go test ./... -race` 15 包全绿；vet 零告警；gofmt 干净；npm lint/build 通过；
    竞品词扫描通过；契约 docs/05 §5.10、口径 docs/04 第九节。
- **M3-wave4 worker + bundle 优化 + 个人日志视角（落于 2026-09-03）——M3 里程碑收官**：
  - internal/worker 极简 ticker 池（手写零新依赖）：Task{Name,Interval,Run} 统一
    Start/Stop 生命周期，goroutine 内 time.Ticker 循环 + 逐次 panic recover 不带崩进程，
    Stop 幂等（once + close + wg.Wait）；两个任务——验证码清扫（1min，调
    verification.Store.Sweep，原 main 内联清扫 goroutine 收编）与订单超时关单（5min，
    条件 UPDATE `status=1 AND created_time < cutoff` 置 4=已过期，cutoff =
    now − `topup.order_timeout_minutes` 缺省 15min 热生效）；main 启动链路统一挂载
    worker 池，停机顺序：HTTP 优雅关闭 → worker 池 Stop → dispatcher.Stop → 存储关闭；
    预算重置维持请求路径惰性重置（不做 worker）；
  - GET /api/log/mine（登录态）：会话作用域（user_id 恒取会话用户，查询参数忽略）+
    logMineView 字段白名单（照 tokenMineView 模式：无 user_id/channel_id——会话即归属、
    渠道为管理面路由语义；保留 token_id/protocol/model_name/tokens/quota/use_time/
    is_stream/detail/created_time）；分页与模型/时间过滤照既有 API 惯例；
  - 前端：Logs 页按角色取数（root 走 /api/log 保持全量 + 用户/渠道列与筛选；普通用户走
    /api/log/mine，隐藏用户/渠道筛选与列、不请求管理面下拉源）；App.tsx 全部页面组件
    React.lazy + Suspense（Spin fallback），vite manualChunks 拆 vendor 与 antd——
    主 chunk 1,321.39 kB（gzip 418.81）→ 15.95 kB（gzip 7.01），13 个页面 chunk
    2-9 kB 按路由懒加载，antd 独立 chunk 1,226.44 kB（gzip 385.59）缓存友好；
    go:embed hash chunk 兼容（smoke 测试覆盖 SPA 回退）；
  - 测试修复：order_test.go seedTopupOrder 的 order_no 加 atomic 序数后缀（Windows
    时钟粒度 ~15.6ms 下 UnixNano 重复撞唯一索引，已录 docs/11）；
  - ADR-0006（商业化开关：options 白名单/敏感值哨兵脱敏/默认关不落库）与 ADR-0007
    （支付回调：双验签差异/金额逐位校验/条件更新幂等/aff 同事务/return 不承担安全边界）；
  - e2e 冒烟 scripts/e2e-smoke.ps1（隔离环境 3100）22/22 全绿：root 配置 → 注册
    （带 root aff_code）→ 登录 → 2FA setup/enable → 二段式登录 → 兑换码生成与核销 →
    /api/log/mine 白名单断言 → /api/log 普通用户 403 → epay 下单 → 手工构造 MD5 签名
    notify 回调 → 买家 quota 恰好入账（+1000 兑换 +694444 充值）→ 订单 status=2 →
    root aff 返利 +69444（10%）；
  - 验收：`go test ./cmd/... ./internal/... ./web/... -race` 全绿（worker 包 4 例、
    api 包 log mine 例新增）；vet 零告警；gofmt 干净；npm lint/build 通过；竞品词扫描通过。
- **M4-wave1 旧库只读纪律 + 迁移工具 + 本地演练（落于 2026-09-04，M4 开篇）**：
  - internal/store.OpenReadOnly（旧库绝对只读双层防御：DSN `mode=ro` + 连接后
    `PRAGMA query_only=1`，只读池限 2 连接；3 测试：写全被拒且文件无污染、缺失文件
    不创建、WAL 源可读）；
  - internal/migrate（transform 纯函数层 + 引擎层）与 cmd/migrate CLI
    （`migrate -legacy <旧库> -target <hui库> [-report out.json] [-export-tokens tokens.tsv]`）：
    六表迁移全部自然键 upsert（users.username / tokens.key_hash / channels.id /
    redemptions.key / logs.id / options.key）幂等可重跑；变换规则硬失败制（bcrypt
    守卫/未知类型与状态/非 envelope param_override 均拒绝该行入报告）；key_hash 复用
    gateway.HashKey 防口径漂移；ModelRatio 按启用渠道模型最小集 ×2 换算（黄金锚定
    85/766/14 三例固化 TestModelRatioGoldenAnchoring，÷2 反证否决）；type=1 日志合成
    topup 对账（面值从 content 正则关联旧兑换码）；内置对账 13 项指标 + options 逐键，
    不等 exit 1；报告 JSON 完全确定性（无时间戳，两遍逐字节一致）；
    QuotaPerUnit≠500000 守卫拒跑；-export-tokens 产物含令牌明文（敏感，用后即删）；
  - scripts/migrate-drill.ps1（PS 5.1 + UTF-8 BOM）：解压最新备份→空库迁移→起
    hui-api 3200 端口（隔离数据目录）→冒烟（/health、/api/status、迁移令牌
    /v1/models、错 key 401、真实转发计费复核，上游不可达如实降级计费引擎层复核）→
    停服清库重跑共 2 遍→两遍报告 diff 确定性；产物存 %TEMP%\hui-m4w1-drill；
  - 验收：`go test ./cmd/... ./internal/... -race` 全绿（migrate 包 transform+集成
    全覆盖、cmd/migrate 4 例）；vet 零告警；gofmt 干净；竞品词扫描通过；演练实跑
    证据见下方交接记录；
  - ADR-0008（旧库只读纪律与幂等覆盖式同步）；docs/03 第七节（工具用法/变换规则表/
    对账语义/报告字段）；docs/11 追加 GORM 主键隐式条件与 SQLite 拼接坑。
- **M4-wave2 生产旁路部署 + 切换 runbook（落于 2026-09-04）**：
  - 服务器旁路落机（2GB 小机型与旧网关并存，四不碰：不 restart/stop 生产服务、不写
    生产库、不动安全组/防火墙、不占 3000 端口）：交叉编译 linux/amd64 三产物
    （hui-api v0.4.0-m4w2 / migrate / logcheck，本地与服务器 MD5 双侧校验）→
    /home/ubuntu/hui-api/{bin,config,data,logs}；config chmod 600（SESSION_SECRET
    服务器端 openssl rand -hex 32 生成一次固化，不落仓库）；systemd 单元 hui-api.service
    （User=ubuntu、ExecStart 带 -config、MemoryMax=400M、GOMEMLIMIT=120MiB、
    Restart=on-failure）enable 后 start，active（Memory 26M/400M）；
  - 数据初始化：服务器最新 cron 备份副本为只读源跑 migrate——对账 18 项全 OK
    （users 3 / tokens 3 / channels 5 / redemptions 3 / logs consume 1010+topup 合成 3；
    sum_quota=692191867882 与本地演练一致）；报告落 logs/migrate-report.json（600）；
  - 冒烟 7/7 PASS（scripts/deploy-smoke-server.sh，HUI_BASE 可覆盖端口供切换日复用）：
    /health 200、/api/status schema=4、RSS 基线、迁移令牌 /v1/models 200 models=5
    （key_hash 口径生产数据成立的关键锚点）、真实转发 glm-5.3-flash usage p=17 c=32
    计费入账 quota=11（与本地演练一致，×2 换算生产数据二次实证）、journalctl 无
    panic/fatal、available 1391MB 双进程并存；生产网关全程 active 且 etime 连续
    （零重启）；敏感临时文件（令牌 TSV/旧库解压副本/远端一次性脚本）用后即删；
  - 文档：docs/06 第七节正式切换 runbook（预检清单/六步切换表含预估耗时与验证命令/
    回滚两条命令 RTO<30s/切换后待办）；ADR-0009（旁路拓扑 127.0.0.1:3100 与双层
    内存限额）；CHANGELOG 同步。
- **下一步**：切换窗口执行 docs/06 第七节正式切换 runbook（预检全绿 → 快照 → 停旧网关
  → 最终迁移（重置目标库）→ 改绑 3000 → 冒烟 → 观察 30min，回滚 RTO<30s）；M5 生产收尾。
- **风险与未决**：验证码为进程内存存储（多副本部署不共享、重启即失效——单副本部署语义
  可接受，多副本挂后续评估）；后台任务统一挂 internal/worker 池（main 装配，停机顺序
  先停 HTTP 再停 worker 再排空网关日志）；前端 bundle 已拆分（主 chunk ~16KB，AntD 独立
  chunk 1.2MB 为库本体与首屏并行加载——页面迭代不再触发库重下载）；熔断状态为进程内存态
  （**持久化方案已评估否决**：破坏半开探测语义，维持重启清零）；Idempotency-Key 头
  **已复核否决**（PUT 整对象幂等已覆盖，见 ADR-0005）；模型广场 /v1/models 依赖手工输入
  转发面令牌；SMTP/Turnstile/OAuth/支付网关均为出站请求，目标地址由管理员经 options
  配置，可达性与信任边界由配置方负责；支付回调（epay notify/stripe webhook）为入站
  公开端点，安全边界由签名验签（恒时比较）+ 金额逐位校验 + 幂等条件 UPDATE 承担；
  订单超时关单 worker 与回调构成单向状态机（过期后完成支付的极小窗口由网关重试+对账
  兜底）；预算窗口惰性重置语义不变；npm install 2 个开发依赖告警（移 M5 处理）；
  内置 prices.json 仅示例价（生产必须经 options 配置真实价，未配价 503 为设计行为）；
  本地 bin/go-sdk 为磁盘遗留杂物（已 .gitignore，建议清理）；迁移对账前提为目标库
  未上线库（切换窗口内执行，docs/03 第七节）；迁移报告的未迁清单（换皮 options、
  model_mapping 缺口、渠道多组丢弃）需切换前人工复核（报告 JSON 内置清单）；旁路库
  数据落后生产 ≤24h（cron 周期），切换日重迁覆盖（docs/06 第七节步骤 3 重置目标库）。
- **最后更新**：2026-09-04

## 交接记录

### 2026-09-04 M4-wave2 收尾（生产旁路部署 + 冒烟 7/7 + 切换 runbook）

- 落地范围：服务器旁路部署（三二进制交叉编译 + MD5 双侧校验、目录布局、config 600、
  systemd 单元 enable + start）、migrate 用服务器最新备份副本初始化旁路库（对账 18 项
  全 OK）、冒烟 7/7 PASS（scripts/deploy-smoke-server.sh 入库，HUI_BASE 可覆盖）、
  docs/06 第七节切换 runbook、ADR-0009。
- 交接注意 1（启动顺序）：systemd enable 与 start 分离——先 migrate 后 start。对账 13 项
  前提是目标库未上线（ADR-0008），先 start 会因 root 引导等启动期写入破坏行数对账；
  旁路冒烟产生的 logs 增量同理——切换日最终迁移前必须重置目标库（docs/06 第七节步骤 3）。
- 交接注意 2（journald 优先级口径）：Go log 默认写 stderr，journald 将服务 stderr 全部
  标为 err 优先级——「无 error 级日志」不能用 `journalctl -p err` 过滤（会把全部正常日志
  命中），以错误关键字（panic/fatal/异常退出）为口径（冒烟脚本 f 项）。
- 交接注意 3（SESSION_SECRET 固化）：config.yaml 已存在时 setup 脚本跳过重生成（幂等
  保护，防止误重生成使全部管理台会话失效）；密钥只在服务器，不落仓库/报告。
- 交接注意 4（备份时序）：cron 04:30 CST 产物文件名含生成日期（YYYY-MM-DD，与生成日
  同日，前缀以服务器备份脚本为准）；切换窗口建议选在 04:30 备份完成后，保证「当日
  最新备份」即「最新数据」。
- 交接注意 5（生产零影响证据）：全程生产服务 active 且 etime 连续（无重启）、3000 绑定
  未变、3100 仅 127.0.0.1；旧库仅以备份副本参与（migrate mode=ro 打开解压副本，生产
  库文件本身未打开）；生产库的日记账（当日增量）不在旁路库内，属预期。
- 交接注意 6（logcheck helper）：bin/logcheck 由部署侧生成编译（bin/ 不入库），与 drill
  的 drill-read 同源逻辑——时间窗/协议过滤在 Go 侧、单对象 JSON 输出；服务器与本地
  编译均置于 .gitignore 的 bin/ 下。
- 遗留：npm install 2 个开发依赖告警未在本波处理（移 M5）；旁路库数据落后生产 ≤24h
  （cron 周期），切换日重迁覆盖；SSH 隧道访问管理台未配置为常态（用户侧手工即可）。

### 2026-09-04 M4-wave1 收尾（只读纪律 + 迁移工具 + 本地演练×2，M4 开篇）

- 落地范围：internal/store.OpenReadOnly（双层只读 + 写失败测试）、internal/migrate
  （transform 纯函数层 + 幂等 upsert 引擎 + 强对账 + 确定性报告）、cmd/migrate CLI
  （-legacy/-target/-report/-export-tokens）、scripts/migrate-drill.ps1（2 遍演练编排）、
  ADR-0008、docs/03 第七节；测试 `go test ./cmd/... ./internal/... -race` 全绿。
- 交接注意 1（换算方向）：ModelRatio legacy→hui 是 **×2** 不是 ÷2——推导：旧 classic
  quota=(p+c·cr)·mr·gr 无 /1e6 环节（mr 内嵌比例），hui 有 ×500000/1e6 环节；令两式对
  同一账单相等即 mr_hui=mr_legacy×2。黄金锚定 85/766/14 固化于
  TestModelRatioGoldenAnchoring，动计费换算必须重跑该测试。
- 交接注意 2（幂等语义）：自然键冲突目标（users.username/tokens.key_hash/channels.id/
  redemptions.key/logs.id/options.key）——glebarez/sqlite 下重跑时 id 与自然键双冲突，
  指定自然键为冲突目标即成功覆盖；冲突目标写主键在脏数据（同键不同 id）下会撞约束
  拒绝（属期望行为，防静默错迁）。覆盖列仅迁移归属列，运行时列（预算/aff/totp）不被旧值覆盖。
- 交接注意 3（对账前提）：13 项指标对账假设目标库为未上线库；切换日先 migrate 后起服务，
  服务产生新数据后不得重跑对账（行数必不等）。
- 交接注意 4（-export-tokens）：产物 TSV 含令牌明文（演练冒烟与切换接管期分发用），
  仅存在本地，用后即删；转发面鉴权只依赖 key_hash，清单丢失不影响网关运行。
- 交接注意 5（演练脚本）：bin/drill-read helper 由脚本生成并编译（bin/ 不入库），
  时间窗/协议过滤在 Go 侧完成、只输出单对象 JSON——PS 侧零数组解析（PS 5.1 管道
  JSON 数组堆叠坑见 docs/11 2026-09-04）；演练产物在 %TEMP%\hui-m4w1-drill
  （含 report-diff.txt/tokens-diff.txt/drill-summary.txt）；真实转发若上游失败脚本
  自动降级为黄金锚定测试并如实注明。
- 交接注意 6（演练实跑证据，2026-09-04）：最新备份解压后 2 遍演练全绿——迁移计数
  users 3 / tokens 3 / channels 5（组丢弃=5、model_mapping 缺口=1：channels id=6
  glm-5.3-flash→z-ai 映射）/ redemptions 3 / logs consume 1010+topup 合成 3 /
  options 迁 5 键未迁 29 键；对账 13 项表级指标 + 5 个 options 键全部 OK
  （users.sum_quota=692191867882、sum_used_quota=89085545、
  logs.consume_sum_quota=89101006、redemptions.sum_quota=6849515068、
  channels.sum_priority=24）；冒烟两遍各 6 项 PASS（/health、/api/status schema=4、
  迁移令牌 /v1/models models=5、错 key 401、真实转发计费复核 usage p=16 c=32 且
  日志 quota=11——**上游真实可达，未降级**）；report/tokens 两遍 diff 均 IDENTICAL
  （报告无时间戳逐字节一致）；×2 换算经真实转发二次实证（迁移渠道 id=7 计费正常
  入账）。

### 2026-09-03 M3-wave4 收尾（worker + bundle 优化 + 个人日志视角，M3 里程碑完成）

- 落地范围：internal/worker ticker 池（验证码清扫 + 订单超时关单）挂载 main 启动链路、
  GET /api/log/mine（会话作用域 + logMineView 白名单）、前端 Logs 按角色取数、App
  React.lazy + manualChunks 分包（主 chunk 1.32MB→16KB）、ADR-0006/0007、e2e 冒烟脚本
  scripts/e2e-smoke.ps1（22/22 全绿）；M3 四波全部完成，下一步 M4。
- 交接注意 1（worker 池语义）：Task Interval≤0 或 Run==nil 静默忽略；runOnce 逐次
  recover（panic 只终结当次执行不退 goroutine）；Stop 幂等可重入；新增后台任务一律加进
  main newRouter 的 workers.Start(...)，勿再手起 goroutine。
- 交接注意 2（订单关单阈值）：`topup.order_timeout_minutes` 在 options 白名单内热生效，
  缺省 15min；worker.ExpireStaleTopupOrders 单条条件 UPDATE，与回调结算互斥语义见
  docs/04 第九节第 4 小节。
- 交接注意 3（logMineView 白名单）：无 user_id（会话即归属）与 channel_id（管理面路由
  语义）；logs 表无 type 列，个人日志即全部计费/核销/返利记录；显式白名单收口后 model
  新增敏感字段不会意外经 mine 端点泄露。
- 交接注意 4（bundle 分包）：manualChunks 双组 vendor/antd + 页面级 lazy；ConsoleLayout
  保持静态导入（骨架壳避免闪屏）；embed 兼容已验证（assets hash 文件名 + SPA 回退 smoke）。
- 交接注意 5（测试坑，已录 docs/11）：Windows 时钟粒度 ~15.6ms，UnixNano 生成的 order_no
  连续 seed 必撞唯一索引——测试种子唯一性一律加 atomic 序数后缀。
- 交接注意 6（e2e 签名坑）：EPay MD5 签名串每对参数带尾 `&`（k1=v1&k2=v2& + key），
  手工构造回调签名时勿写成 k1=v1&k2=v2 + key（验签恒时比较必 fail）。

### 2026-09-03 M3-wave3 收尾（支付对接 + 邀请返利，Task #21）

- 落地范围：internal/payment 适配层（epay MD5 / stripe Checkout+Webhook，零 SDK 依赖）
  + order.go 六端点与共享结算事务 + aff.go 邀请信息 + 前端充值页在线区块/订单列表与
  Invite 页；契约 docs/05 §5.10 与错误码表、口径 docs/04 第九节；wave2 遗留项加固
  （预算并发测试排空断言 + mailer gofmt 复验）随波清偿（commit 5bc6633）。
- 交接注意 1（结算幂等模型）：与兑换码核销同一「条件谓词 + RowsAffected」模型——
  条件 UPDATE `status 1→2 WHERE id AND status=1` 竞争失败即重复通知，幂等跳过且仍回
  success（网关停止重试）；三重前置校验（网关/币种/金额逐位）在 UPDATE 之前，失败
  不动账；epay notify 纯文本应答不暴露内部错误细节（success/fail 二值）。
- 交接注意 2（rate 定点快照语义）：订单 rate 列 = 「每 1 USD 的 CNY 分数×100」——
  epay 记 topup.usd_cny_rate（720=¥7.20/$1），stripe 恒 100（USD 本位）；换算
  (cents×500000+rate/2)/rate 整数四舍五入；快照与金额同记防改价漂移；aff 返利比例
  结算时读运行轨不随订单快照（改比例只影响后续新结算）。
- 交接注意 3（Stripe 不落库防孤儿单）：epay 先落库后拼跳转 URL（纯本地计算无外部
  依赖）；stripe 先调 Checkout API 成功再落库——API 失败 502 gateway_create_failed
  且不产生订单，防「有单无 session」孤儿；webhook 结算失败映射 4xx/5xx 让 Stripe
  重试（invalid_signature 400 / order_mismatch 400 / settle_failed 500）。
- 交接注意 4（测试注入点 wave3 增量）：h.stripeHTTP/h.stripeAPIBase 指向 httptest 假
  Stripe 端点断言请求形态（form 编码/Basic 鉴权/metadata.order_no）；epay notify 测试
  用 payment.EPaySign 闭环构造 query（enableEpay helper 注入密钥）；并发幂等复用
  12 goroutine start-channel 模式（notify 为公开端点无 IP 限频，无需独立 RemoteAddr）。
- 交接注意 5（前端回跳查单）：epay return 与 stripe success_url 都落
  /console/topup?order=<order_no>，前端从订单列表首页回查该单（order_no 全局唯一，
  回跳单必为最新订单，首页命中）；回调与回跳存在竞速，未命中提示刷新不轮询——
  展示层不做轮询定时器，刷新按钮手动收敛。

### 2026-09-03 M3-wave2 收尾（OAuth + 2FA + 个人中心，Task #20）

- 落地范围：OAuth 通用 provider（oauth.go）+ TOTP 三端点（totp.go）+ 二段式登录
  （session Stage claim + login/2fa）+ 个人中心（profile.go）+ 前端三页 + 17 个新
  测试；契约 docs/05 §5.8/§5.9；权衡注记 docs/11（redirect_uri 信任链、userinfo
  不验 ID token 签名、bind 会话语义）。
- 交接注意 1（bind 模式会话语义）：bind 模式 callback **永不签发/顶替会话**——身份
  已绑本人幂等成功、已绑他人失败路径（复合唯一冲突）；发起时 state cookie 标
  bind+uid，回调时校验当前会话 uid 一致（双保险防 cookie 串改）。
- 交接注意 2（防锁死双路径）：无 password_hash 用户解绑最后一个身份 → 400
  identity_last；个人中心首次设置口令免验旧口令（会话即授权依据），先设密即可解绑。
- 交接注意 3（stage1 防提权）：stage1 会话（TTL 5min）仅可访问 /api/user/login/2fa，
  RequireAuth 校验 Stage==0 否则 401 totp_required；旧 cookie 无 stage 字段反序列化为
  0 向后兼容；login/2fa 与密码登录共用 login|<IP> 限频（防 stage1 重放爆破动态码）。
- 交接注意 4（测试注入点 wave2 增量）：Handler 新增 oauthHTTP（*http.Client）与
  oauthGithubAuthorizeURL/TokenURL/UserinfoURL、oauthLinuxDOIssuer 同包小写字段，测试
  直接赋值指向 httptest 假 provider（fakeGithub）；OIDC 发现走 oauth.oidc.issuer 配置
  键指向假服务器，断言发现文档仅抓一次（包级缓存 1h）。
- 交接注意 5（wave2 测试坑，已录 docs/11）：oidc 发现闭包引用 issuerURL 必须先 var
  声明后赋值（:= 声明在 handler 注册之后则闭包标识符未定义，编译错误）；pquerna/otp
  生成的 otpauth URI issuer 为 URL 编码形态 `issuer=Hui%20Api`（非 `+`，断言按 %20 写）。
- 下一步 M3-wave3：支付对接（EPay/Stripe，topup_orders 消费方，订单状态机见 schema v4）。

### 2026-09-03 M3-wave1 收尾（schema v4 与公开注册体系）

- schema v4（users 5 列 + user_identities/topup_orders）+ options 商业化白名单/脱敏
  哨兵 + mailer/verification 新包 + 公开注册四端点 + /api/status features + 测试矩阵
  全绿；下一步 M3-wave2（OAuth 登录 + 支付对接，两张新表的消费方）。
- 交接注意 1（公开组安全边界）：四端点无会话要求，安全边界 = 开关门控（register.enabled
  缺省关，关闭时 403 register_disabled）+ IP 限频（Handler.authRL 独立 ratelimit 实例，
  与转发面限流器隔离，被拒不影响转发配额；键 register|<IP>/login|<IP>/reset|<IP>，
  1h 窗口 5/10/5，超限 429+Retry-After）+ Turnstile/邮箱验证码可选强制（enabled 开即
  强制，用可选能力前先在 /api/setup 探测）；重置密码 404 泄露面权衡：邮箱未绑定时码已
  消费，枚举需先获取有效码。
- 交接注意 2（aff 双向奖励原子性）：建户与奖励同一事务（建户 → gorm.Expr 原地累加邀请人
  quota+aff_history_quota → 新用户 quota → 两条 protocol="aff" logs），任一步失败整体
  回滚；并发同邀请人靠 SQLite 单写池串行化 + Expr 原地累加保证恰 N×reward（
  TestRegisterAffConcurrent 10 goroutine 断言）；奖励值 ≤0 不入账不写日志；aff_code
  无效/自引用不阻断注册。并发测试需为每 goroutine 独立 RemoteAddr（httptest 默认
  192.0.2.1 共享 IP 会触发注册限频 429）。
- 交接注意 3（验证码存储语义）：进程内存 map（email+purpose 隔离键），6 位数字，
  TTL 10min/同邮箱重发 60s/每日 20 封/一次性消费（mismatch 不消费正确码，可重试至
  过期）；清扫 = Sweep() 惰性 + StartVerificationSweeper 定时（main 装配 1min，返回
  stop 闭包；停机顺序先停清扫器 → 排空网关日志 → 关 HTTP → 关存储，与 defer LIFO
  对应）；**多副本部署不共享、重启即失效**（单副本可接受，多副本挂后续评估）；码不
  回显、不落日志。wave2 落地 OAuth/多副本前若需持久化，从 store 层新表扩展，勿复用
  options。
- 交接注意 4（测试注入点）：Handler 依赖为同包小写字段——authRL（限频器）、verifier
  （TurnstileVerifier 接口）、mailer（Mailer 接口）、vstore（*verification.Store）——
  api 包测试直接赋值替换；verification 测试用 fakeClock 注入时钟驱动 TTL/限频/每日
  窗口；mailer 测试用自签 ECDSA 证书起 TLS listener 假 SMTP 服务器（勿宣告
  SMTPUTF8/8BITMIME，见 docs/11）。
- 交接注意 5（options 脱敏/哨兵契约）：GET /api/option 对键含 password/secret（不区分
  大小写）的值恒回 `******`（库内明文不变，仅出口脱敏）；PUT 值 `******` 为哨兵跳过
  不覆盖（脱敏回显幂等；哨兵键仍须过白名单与长度校验）；新增敏感键无需特殊处理（按键名
  自动脱敏），新增可写键仍须前后端双侧同步（Settings.tsx KEY_GROUPS 已含 8 个新分组）。
- 交接注意 6（GORM 测试坑，已录 docs/11）：复用已填充 struct 作 First 参数会附加旧主键
  条件（record not found）——同一变量二次查询前必须重置（row = model.Option{}）。

### 2026-09-03 M2 收官 Medium 修复与 e2e 清理（Task #19）

- 普通用户看板 Medium 缺陷清偿 + e2e 环境验证与收尾清理；M2 里程碑正式收官，下一步 M3。
- 交接注意 1（自服务统计端点设计）：/api/user/stats 服务端 SQL 聚合（COUNT/SUM/GROUP BY）
  而非前端分页拉日志聚合——一次请求无截断上限（模型分布封顶 100 行防响应体膨胀），
  「今日」口径以服务器本地时区为准（与浏览器本地 startOf('day') 在跨时区部署时可能有
  分钟级偏差，单时区部署无感）；SUM 空集 COALESCE 归零保证无日志用户拿到全零空态而非
  500；作用域恒为会话用户（user_id 查询参数忽略），不新增越权面。
- 交接注意 2（优雅降级口径）：看板两类数据源加载失败均降级空态（0 值卡片 + 空表 +
  console.warn 留痕），不显示错误横幅——看板是概览页，失败横幅的惊扰大于价值；但 root
  路径的「截断」提示保留（数据不完整的明示义务）。
- 交接注意 3（e2e 环境状态）：验证完成后 e2e 进程已停止（端口 3100 释放）；scripts/
  e2e-run.ps1 与 bin/e2e-data/e2e.db（含 Task#18 种子用户 alice/root 与请求日志）保留供
  M3 验收复用；alice 口令已重置为脚本可控值（隔离 e2e 库测试口令，非生产凭据）。重启
  流程同 Task#18：Stop-Process 旧 hui-api PID → 后台运行脚本 → /health 200。

### 2026-09-03 M2 缺陷修复（Task #18 浏览器验收）

- 四项验收缺陷修复 + e2e 重启全部落地；M2 收官后零功能新增，均为验收缺陷清偿。
- 交接注意 1（守卫探针教训，已录 docs/11）：控制台会话探针必须用最低权限端点——原用
  GET /api/option（root 专属）把普通用户 403「需要管理员权限」误判为会话失效；改用
  GET /api/user/self 后探针响应顺带返回角色（前端 saveSession 刷新元数据，角色以服务端
  为准，localStorage 不再是角色权威）。新增前端探针先核对端点鉴权层级。
- 交接注意 2（所有权作用域端点设计）：/api/token/mine 强制取会话用户（user_id 查询参数
  忽略），响应为 tokenMineView 显式白名单（无 key/key_hash/tpm_rpm/tags/allow_ips）——
  用户自视端点不要直接复用 model 序列化：显式白名单收紧后，model 新增敏感字段不会意外
  泄露。管理列表 GET /api/token 保持 root 专属不变。
- 交接注意 3（gin 路由共存）：GET 树下 /api/token（管理组）与 /api/token/mine（公开组）
  静态段共存无冲突；同方法树静态与参数段混挂依赖 gin ≥1.7（既有 POST /api/token 与
  POST /api/token/:id/assign 已依赖）。
- 交接注意 4（e2e 服务流程）：scripts/e2e-run.ps1 已入库（隔离 bin/e2e-data/e2e.db，
  root/e2eRoot2026，端口 3100）；重启流程 = Stop-Process 旧 hui-api PID → 后台运行脚本
  → /api/status 200 → 保持运行供浏览器复验；冒烟脚本留存于外部工作区
  (.tmp-task18/work/smoke-task18.ps1)，验证普通用户四端点行为矩阵（self 200 /
  mine 200 无泄露 / option 403 / 管理列表 403）。

### 2026-09-03 M2-wave3 收尾（M2 里程碑完成）

- 兑换码核销状态机 + 令牌预算惰性重置 + hooks 首批 + 流中断部分结算 + 充值页全部落地；
  M2 三波收官，下一步 M3。
- 交接注意 1（核销原子性与惰性过期）：核销 = 事务内「条件 UPDATE status 未用→已用 +
  入账 + 日志」三步，任何一步失败整体回滚；并发正确性靠 SQLite 单写池串行化 + 条件谓词
  （RowsAffected=0 即竞争失败），与 billing.Ledger 同一模型——证据 TestTopupConcurrentAtomic
  （12 goroutine 抢同码恰一人 200）。过期码惰性标记走「标记 + return nil」模式：若在事务内
  return err，标记会随回滚丢失，下次重复校验——状态迁移标记类副作用必须在事务内提交，
  拒绝响应放事务外映射。
- 交接注意 2（预算窗口语义）：budget 惰性重置在请求路径（rollBudget，鉴权后），非定时器
  ——无请求不回补，语义为「下次请求时对齐窗口」；BudgetResetAt=0 首次请求 CAS 只定界
  不重置；过期逐窗口步进保相位（cursor 从旧边界推进到 now 之后，monthly 走 addMonths
  Clamped 月末钳制）；CAS `WHERE budget_reset_at=旧值` 保证并发恰一次重置（8 goroutine 恰
  一条 budget_reset 日志）；窗口内消耗受 remain_quota 封顶（与预扣费账本共用一列，重置 =
  remain 复原为 quota）；assign 划转同步加 remain+quota 保持 remain≤quota 不变式，别只加
  remain（会破坏封顶语义）。
- 交接注意 3（hooks 语义）：事件投递为异步旁路（Dispatcher 有界队列 + panic 隔离），任何
  hook 失败只丢弃计数不阻塞转发；completed/failed 事件带 request_id 与 idempotency_key
  （requestID:eventType）；OTLP 为累计 temporality（startTime 固定全量累计），int64 字段按
  protobuf JSON 规范字符串编码；hooks.* 三键已入 options 白名单，前端 Settings 已同步——
  新增可写键必须双侧同步（见 wave2 交接注意 3）。
- 交接注意 4（部分结算口径）：「已发生即已消耗」三分支——usage 有值按用量（partial）；
  usage 缺失但 c.Writer.Size()>0 按字节粗估（partial+estimated，输入按请求体 /4、输出按已写
  字节 /4）；两者皆无全额退款。usage 证据优先于粗估：anthropic message_start 先携带输入
  用量，流中断后仍按 usage 结算（TestGatewayStreamAbortPartialUsage）；openai 流断连无
  usage 走粗估（PartialEstimated）；非流式读体失败零写入走全额退款（Refund）。
- 交接注意 5（结算重构与渠道回填）：settle 抽出 chargeAndSettle/refundFullDetail 两个
  helper，三分支与正常路径共用；日志 ChannelID 回填实际服务渠道（retry pick 后赋值
  logChannel，兜底失败路径为 0）——wave1 遗留的「日志无渠道 ID」已清偿，日志渠道过滤与
  看板渠道分布自此生效。

### 2026-09-03 M2-wave2 收尾（前端控制台八页面）

- 前端 src/api 客户端 + react-router 路由 + ConsoleLayout 会话探针 + 八页面全部落地并真实
  冒烟；关键对齐点沉淀如下。
- 交接注意 1（整对象写的前端义务）：PUT 全量覆盖，前端表单必须显式携带全部业务字段（含零
  值）——渠道 key 留空=保旧（脱敏回显不可回填）；令牌编辑显式携带 remain_quota 防误重置；
  用户编辑自己时 role/status 不渲染表单项，payload 必须原值回传，否则后端 self_lockout 检查
  会拦截任何编辑（含改邮箱）。
- 交接注意 2（鉴权双体系）：管理面靠 HttpOnly 签名 cookie（fetch credentials:'same-origin'，
  localStorage 只存展示用元数据）；/v1/models 是转发面端点，Bearer 令牌鉴权与管理台会话无
  关——模型广场页令牌仅存 localStorage；api 客户端 401 统一跳 /login（登录接口豁免，防密码
  错误误跳）。
- 交接注意 3（options 键白名单双侧同步）：系统设置页 KEY_GROUPS 与 internal/api/option.go
  allowedOptionKey 对齐（relay.*/billing_setting.* 前缀 + ModelRatio/CompletionRatio/
  GroupRatio/ModelRequestRateLimit* 五精确键）；换皮类键（SystemName 等）后端不支持，前端不
  提供编辑，新增可写键必须双侧同步，否则前端保存即 400 option_forbidden。
- 交接注意 4（构建链）：lint 脚本为 tsc --noEmit（strict + noUnusedLocals 全开，新增代码注意
  未使用导入即报错）；JSX 属性字符串不支持反斜杠转义（`\"` 会提前终止字符串），含引号文案
  用 `attr={'JS 字符串'}` 形式；CI web job 走 npm ci + build，bundle 1.27MB 优化挂 M3。

### 2026-09-03 M2-wave1 收尾（管理面 API 与限流挂接）

- 管理面 API 六组端点 + 会话鉴权 + internal/ratelimit 限流挂接 + 令牌分组接 GroupRatio 全部落地；关键决策沉淀于 ADR 0005（bcrypt 采用 x/crypto 而非自写、HMAC 签名 cookie、auth_version 失效机制、限流与熔断解耦）。
- 交接注意 1（会话与 auth_version，改动必读）：会话 cookie = base64url(payload).base64url(HMAC-SHA256)；payload 含 uid/authv/exp，**auth_version 在中间件层与库比对**（不在签名层）——改密递增 auth_version 后旧 cookie 签名仍合法但比对失败 401；SESSION_SECRET 缺省随机生成（重启全端会话失效），生产在配置文件固定；root 引导幂等（已存在任何 root 则跳过）。
- 交接注意 2（整对象幂等写）：管理面 PUT 一律全量覆盖（显式字段含零值生效，防裸对象丢 priority/weight）；缺省归一化规则——status 0→启用、token.expired_time 0→永久、group 空→default、user.role 0→普通用户；channel.key 空=保留旧值（唯一例外，避免 PUT 回显脱敏值覆盖明文）；token 的 key/key_hash/user_id、root 自身的 role/status（防自锁）不可经 PUT 修改。
- 交接注意 3（写后失效挂接点）：渠道增删改后 `gw.Breaker().Reset(id)`（渠道调度无缓存，失效语义即复位熔断计数）；令牌增删改后 `gw.Auth().Invalidate(keyHash)`（鉴权缓存对 invalid 结果也缓存，必须显式失效）；删除用户级联删令牌并逐个失效——两处挂接共享同一 Gateway 实例（main 装配保证），**测试验证失效行为必须用 h.gw.Auth() 同一实例，独立 NewTokenAuth 实例缓存隔离测不出挂接缺失**。
- 交接注意 4（限流语义）：身份键——全局 "g"、分组 "grp:<组名>"（均拼 ClientIP），令牌 "tok:<id>"；分组配置覆盖全局且共用周期；**被拒请求不记录不消耗配额**；RecordSuccess 与 AllowRequest 分离（SuccessCount 语义）；429 响应带 Retry-After；本机限流 429 不计入上游熔断失败计数（避免自家限流触发上游熔断）。
- 交接注意 5（options 热更与测试纪律）：/api/option PUT 键白名单（relay.*/billing_setting.* 前缀 + ModelRatio/CompletionRatio/GroupRatio/ModelRequestRateLimit* 五精确键）任一非法整体拒绝，写后 Runtime.Reload() version+1；管理面改动必须跑 `go test ./... -race`；凡断言「写后生效」的测试须断言 Runtime.Version() 递增，防只验证内存值未验证热更链路。

### 2026-09-02 M1-wave3 收尾（M1 里程碑完成）

- 计费内核三模式 + 结算挂接 + 内置价单 + 异步日志 + 黄金测试集全部落地；关键决策沉淀于 ADR 0004（expr 选型、计费口径唯一化、tier 单层语义、未配价 503）。
- 交接注意 1（计费口径，改动必读）：tiered_expr 变量为原始 tokens 数——p=纯输入（input−cache_read）、c=输出、cr=缓存读；系数为每百万 tokens 美元价；表达式值单位 micro-USD；quota = round(值 × 500000 / 1e6)。tiered_expr 不叠乘 group_ratio（表达式即终价）；classic_ratio 与 per_call 叠乘 group_ratio。任何计费改动必须全量跑黄金测试集（`go test ./internal/billing/ -run TestGolden`）。
- 交接注意 2（配置键与查找顺序）：options 键 `billing_setting.billing_mode` / `billing_setting.billing_expr` / `billing_setting.billing_price` + `ModelRatio` / `CompletionRatio` / `GroupRatio`（classic 回退价与组倍率）；LookupPrice 顺序「显式声明 > ModelRatio 隐式 classic > 内置 prices.json > ErrUnpriced」。GroupRatio 语义：组名→值；请求组未配置时回退 default 组值，再回退 1.0。
- 交接注意 3（挂接点）：gateway.Serve 中 Freeze 在转发前（余额不足 403 insufficient_quota，无副作用）；Respond 成功后 Settle 多退少补（补扣允许透支到负数，负数余额拒绝后续请求）；转发失败/流中断 RefundFull + 日志标记 aborted；usage 缺失但有正常内容 → 请求体字节/4 粗估 + estimated 标记；unlimited 令牌 Freeze/Settle/RefundFull 全跳过（日志照记实结 quota，detail.Unlimited=true）。
- 交接注意 4（异步日志）：logs 表写入走 AsyncLogWriter（有界 channel 非阻塞 Submit，队列满丢弃并计数）；停机必须调 `Gateway.Close()` 排空日志（main 已 defer gw.Close()）；测试中先 g.Close() 再查库（billing_test helper logRowsAfterClose）。logs.detail 与 Request/Token 字段强类型，不混用字符串拼 JSON。
- 交接注意 5（热更）：管理面写 options 后调用 `config.Runtime.Reload()` 即热生效——billing settingsCache 按 Runtime 版本号失效重建，表达式编译缓存按表达式文本缓存跨热更复用；prices.json 校验含零用量试求值，新增表达式能力（如多层 tier）必须同步该校验。

### 2026-09-02 M1-wave2 收尾

- 双协议转发与路由编排全部落地；wave1 目标目录名 internal/channel 落地为 internal/gateway（编排与渠道调度/熔断同包，避免过早拆分），docs/01 目录结构已同步。
- 交接注意 1：schema 现为 v2（channels.param_override 列，migrations/0002）；TestDDLEquivalence 已升级为全迁移脚本对账，新增迁移必须同步该测试。
- 交接注意 2：渠道选择/鉴权/熔断的注入点已预留——`Gateway.Breaker()`（M3 管理面复位按钮）、`Gateway.Auth().Invalidate/InvalidateAll`（管理面写令牌后必须调用，否则缓存 30s 才收敛）。
- 交接注意 3：wave3 计费挂接点在 gateway.Serve 的 Respond 返回值（relay.Usage 已归一，CacheReadTokens 已含在 PromptTokens 内并单独标注）；logs 表写入与 hook 事件在 Serve 收尾处接入。
- 交接注意 4：熔断失败率判定只在 OnFailure 时触发（OnSuccess 不判定）；trip 后连续计数清零，半开探测失败需重新累计达阈值才重新冷却——写管理面「复位」或改参数时注意该语义。

### 2026-09-02 M1-wave1 收尾

- 存储层/配置双轨/hook 旁路/管理台外壳全部落地，关键决策沉淀于 ADR 0003。
- 交接注意 1：schema 变更一律新版本迁移（禁改 0001 基线），`store.SchemaVersion` 常量随行递增。
- 交接注意 2：管理面写 options 后调用 `config.Runtime.Reload()` 热生效；options 只存非默认值。
- 交接注意 3：go:embed 硬依赖 web/dist——本地 build.ps1 已自动兜底；CI go job 已加占位步骤，改动 CI 时勿删。
- 交接注意 4：relay 动工时令牌鉴权入口在 tokens.key_hash（SHA-256 hex），限流配置解析在 tpm_rpm JSON 列。

### 2026-09-02 M0 收尾

- 仓库 `github.com/1923256780/hui-api` 建立并推送，Apache-2.0。
- 关键决策：ADR 0001（技术栈 Go+React+Gin+GORM+纯 Go SQLite+go:embed）、ADR 0002（clean-room 独立实现与兼容边界）。
- 交接注意：M1-wave1 动表结构前先读 docs/03，迁移框架按其第四节规则实现。
