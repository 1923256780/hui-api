# 10-里程碑与进度

> 本文件是进度唯一事实源。「当前状态」块是每次会话的交接锚点：开始时读，结束时写。

## ROADMAP

| 里程碑 | 波次 | 要点 | 状态 |
| --- | --- | --- | --- |
| M0 | - | 建仓与环境准备：工程骨架、文档体系、CI、竞品词拦截、Go 环境、最小可运行入口（/health） | ✅ 完成（2026-09-02） |
| M1 | wave1 | 存储层：六表模型与迁移框架、options 双轨热更 | ✅ 完成（2026-09-02） |
| M1 | wave2 | relay：双协议端到端转发、渠道路由与熔断 | ⬜ 未开始 |
| M2 | - | 计费内核：三模式、预扣费与多退少补、流中断退款、未配价硬拒绝、黄金测试集 | ⬜ 未开始 |
| M3 | - | 令牌预算、兑换码、用户体系、管理面 API | ⬜ 未开始 |
| M4 | - | React 管理台（go:embed）+ 端口接管切换演练 + 迁移工具 | ⬜ 未开始 |
| M5 | - | 生产收尾：备份运维常态化、可观测完善、文档定稿 | ⬜ 未开始 |

> 各里程碑的详细设计见 docs/00（总路线）、docs/01（架构）、docs/03-05（领域设计）、docs/06（部署）。

## 当前状态

- **已完成**：M1-wave1——internal/model 六表模型；internal/store 读写分离存储层（写池单连接 + 读池独立，WAL/NORMAL/busy_timeout=5000）与迁移框架（AutoMigrate 基线 + schema_version）；migrations/0001_init.sql 文档源 DDL（TestDDLEquivalence 固化双源一致）；internal/config 配置双轨（env+YAML bootstrap、options 原子快照热更骨架）；internal/hook 有界队列旁路（丢弃计数、per-hook panic 隔离、noop/console 实现）；web/ 管理台外壳（Vite+React18+TS+AntD，go:embed 预留）；main 装配与优雅停机 + /api/status；ADR 0003。DoD 全绿（vet/test/build/npm build/竞品词扫描）。
- **下一步**：M1-wave2——internal/relay 双协议端到端转发、internal/channel 渠道路由与熔断（动工前先读 docs/01 设计点 2/3 与 docs/05 端点清单）。
- **风险与未决**：仓库工作目录内存在两个历史遗留脚本 deployhomev4.sh / prehomev4.sh（未跟踪、含竞品词，已从提交中移除，建议移出仓库目录或删除）；npm install 报 2 个开发依赖告警（未影响构建，M4 前处理）。
- **最后更新**：2026-09-02

## 交接记录

### 2026-09-02 M1-wave1 收尾

- 存储层/配置双轨/hook 旁路/管理台外壳全部落地，关键决策沉淀于 ADR 0003。
- 交接注意 1：schema 变更一律新版本迁移（禁改 0001 基线），`store.SchemaVersion` 常量随行递增。
- 交接注意 2：管理面写 options 后调用 `config.Runtime.Reload()` 热生效；options 只存非默认值。
- 交接注意 3：go:embed 硬依赖 web/dist——本地 build.ps1 已自动兜底；CI go job 已加占位步骤，改动 CI 时勿删。
- 交接注意 4：relay 动工时令牌鉴权入口在 tokens.key_hash（SHA-256 hex），限流配置解析在 tpm_rpm JSON 列。

### 2026-09-02 M0 收尾

- 仓库 `github.com/1923256780/hui-api` 建立并推送，Apache-2.0。
- 关键决策：ADR 0001（技术栈 Go+React+Gin+GORM+纯 Go SQLite+go:embed）、ADR 0002（clean-room 独立实现与兼容边界）。
- 交接注意：M1-wave1 动表结构前先读 docs/03，迁移框架按其第四节规则实现。
